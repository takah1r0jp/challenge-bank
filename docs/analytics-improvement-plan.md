# ダッシュボードアナリティクス改善プラン

> **作成日**: 2025-11-26
> **ステータス**: 承認済み
> **対象**: Failure Bank フロントエンド・バックエンド

## 概要

現在のダッシュボードは統計情報を数字のみで表示しており、視覚的な魅力とユーザーのモチベーションが不足しています。このプランでは、Recharts（既にインストール済み）を活用した視覚化と、ゲーミフィケーション要素を段階的に実装することで、ユーザーの継続的な記録を促進します。

## 現状分析

### フロントエンド
- Recharts 3.5.0 インストール済みだが未使用
- 統計表示は数字のみ（failure_count, total_score, average_score）
- `/stats/calendar` APIは実装済みだが未活用
- date-fns、react-day-picker 利用可能

### バックエンド
- `GET /stats/summary` - 全期間/今週/今月の統計（実装済み）
- `GET /stats/calendar` - 月別日次統計（実装済み）
- `GET /failures` - 失敗記録一覧（実装済み）

## 推奨アプローチ：段階的実装（ユーザー選択に基づく）

### ✅ 選択された実装パス

**フェーズ1**: Pattern 1（視覚化のみ）から開始
**フェーズ2**: ストリーク機能を優先的に実装

この組み合わせにより、最速で視覚的改善を提供しつつ、習慣形成に最も効果的なストリーク機能を早期に導入できます。

### フェーズ1: Pattern 1 - 即効性のある視覚化（最優先実装）

**実装期間**: 1-2週間
**実装コスト**: 13-17時間（フロントエンドのみ）
**バックエンド変更**: 不要
**モチベーション向上**: ⭐⭐⭐

#### 実装内容

1. **週次トレンドライングラフ**
   - 過去7-14日の失敗記録数の推移を視覚化
   - Recharts LineChart使用
   - データソース: `GET /stats/calendar`

2. **スコア分布円グラフ**
   - 挑戦レベル（1-5点）の分布を可視化
   - Recharts PieChart使用
   - データソース: `GET /failures?limit=100`

3. **カレンダーヒートマップ**
   - GitHub風の貢献カレンダー
   - 日別の活動を色の濃淡で表現
   - クリックで該当日の詳細表示
   - データソース: `GET /stats/calendar`

4. **強化版統計カード**
   - トレンド矢印（↑↓）追加
   - 前週比表示
   - データソース: `GET /stats/summary`

#### 必要な新規ファイル
- `components/dashboard/WeeklyTrendChart.tsx`
- `components/dashboard/ScoreDistributionChart.tsx`
- `components/dashboard/CalendarHeatmap.tsx`
- `components/dashboard/ChartSkeleton.tsx`（ローディング状態）

#### 変更が必要な既存ファイル
- `app/(dashboard)/page.tsx` - チャート統合
- `components/dashboard/StatsCard.tsx` - トレンド指標追加

#### 追加パッケージ
```bash
npm install framer-motion
```

### フェーズ1.5: ストリーク機能の優先実装（Pattern 1完了後すぐ）

**実装期間**: 3-5日
**実装コスト**: 8-12時間（フロント5-7h + バック3-5h）
**モチベーション向上**: ⭐⭐⭐⭐⭐

ユーザー選択により、Pattern 2の中からストリーク機能を優先的に実装します。ストリークは習慣形成に最も効果的な機能であり、早期導入が推奨されます。

#### 実装内容

**ストリークトラッカー** 🔥
- 現在の連続記録日数表示
- 最長ストリーク記録
- 視覚的な炎のアイコンとアニメーション
- ストリーク途切れアラート（オプション）

#### 必要な新規バックエンドAPI

```
GET /stats/streak

Response:
{
  "success": true,
  "data": {
    "current_streak": 7,
    "longest_streak": 21,
    "last_failure_date": "2025-01-26"
  }
}
```

#### 実装ファイル

**バックエンド**:
- `backend/main.py` - `/stats/streak` エンドポイント追加
- `backend/tests/test_stats.py` - ストリークテスト追加

**フロントエンド**:
- `frontend/components/dashboard/StreakTracker.tsx` - 新規作成
- `frontend/app/(dashboard)/page.tsx` - StreakTracker統合

### フェーズ2: その他のゲーミフィケーション（長期的、オプション）

**実装期間**: 2-3週間
**実装コスト**: 38-49時間
**モチベーション向上**: ⭐⭐⭐⭐

注：ストリーク機能はフェーズ1.5で先行実装されるため、このフェーズには含まれません。

#### 実装内容

1. **目標ダッシュボード** 🎯
   - 月間目標設定（回数、スコア）
   - 進捗バーと達成率
   - 目標達成時の祝福UI

2. **達成バッジシステム** 🏆
   - マイルストーンバッジ（10回、50回、100回など）
   - バッジ一覧と進捗表示
   - 次のバッジまでの距離

3. **週次サマリー** 📊
   - パーソナライズされたインサイト
   - 先週との比較
   - おすすめアクション

#### 必要な新規バックエンドAPI

##### 1. 目標管理API
```
POST /goals
Request:
{
  "period": "monthly",
  "target_type": "count",
  "target_value": 20,
  "start_date": "2024-01-01"
}

GET /goals
Response:
{
  "success": true,
  "data": {
    "active_goals": [
      {
        "id": "...",
        "period": "monthly",
        "target_type": "count",
        "target_value": 20,
        "current_value": 15,
        "progress_percentage": 75.0,
        "start_date": "2024-01-01",
        "end_date": "2024-01-31"
      }
    ]
  }
}

PUT /goals/{goal_id}
DELETE /goals/{goal_id}
```

##### 2. 達成バッジAPI
```
GET /achievements

Response:
{
  "success": true,
  "data": {
    "unlocked": [
      {
        "id": "first_failure",
        "name": "はじめの一歩",
        "description": "初めての失敗を記録",
        "icon": "🎯",
        "unlocked_at": "2024-01-01T10:00:00"
      }
    ],
    "locked": [
      {
        "id": "streak_7",
        "name": "7日連続",
        "description": "7日連続で記録",
        "icon": "🔥",
        "progress": 5,
        "requirement": 7
      }
    ]
  }
}
```

#### 必要な新規データベースモデル

```python
# models.py に追加

class Goal(Base):
    __tablename__ = "goals"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    period = Column(String, nullable=False)  # "weekly", "monthly"
    target_type = Column(String, nullable=False)  # "count", "score"
    target_value = Column(Integer, nullable=False)
    start_date = Column(Date, nullable=False)
    end_date = Column(Date, nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=func.now())

class Achievement(Base):
    __tablename__ = "achievements"

    id = Column(String, primary_key=True)  # "first_failure", "streak_7"
    name = Column(String, nullable=False)
    description = Column(Text, nullable=False)
    icon = Column(String, nullable=False)
    requirement_type = Column(String, nullable=False)  # "count", "streak", "score"
    requirement_value = Column(Integer, nullable=False)

class UserAchievement(Base):
    __tablename__ = "user_achievements"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    achievement_id = Column(String, ForeignKey("achievements.id"), nullable=False)
    unlocked_at = Column(DateTime, default=func.now())
```

## 実装の優先順位と判断基準

### Pattern 1 → Pattern 2 の段階的実装を推奨する理由

1. **リスク最小化**
   - Pattern 1はバックエンド変更不要で実装可能
   - 既存機能に影響を与えない
   - 問題発生時のロールバックが容易

2. **早期の成果実現**
   - 1-2週間で視覚的改善を提供
   - ユーザーフィードバックを早期に取得
   - モチベーション向上効果を検証

3. **データ駆動の意思決定**
   - Pattern 1の効果測定後、Pattern 2の優先度を判断
   - ユーザーエンゲージメント指標でROI確認
   - 実装コスト vs 効果のバランス評価

### Pattern 1 実装後の成功指標

以下の指標が達成された場合、Pattern 2へ進む：
- ダッシュボード滞在時間: +25%以上増加
- 週次リピート率: +20%以上増加
- ユーザーからのポジティブフィードバック: 65%以上
- 技術的問題: なし

## その他の検討パターン（参考）

### Pattern 3: AI分析とインサイト（長期的）

**実装コスト**: 111-148時間
**必要な追加API**: OpenAI API統合、高度な統計分析

- 失敗パターンのAI分析
- パーソナライズされた改善提案
- 予測分析（目標達成確率など）

### Pattern 4: コミュニティ機能（将来ビジョン）

**実装コスト**: 228-296時間
**必要な追加機能**: ソーシャル機能、プライバシー設定

- 失敗の共有機能
- コミュニティランキング
- 相互励まし機能

## 技術的考慮事項

### Material Design 3 準拠
- マイクロインタラクションの活用
- カラーシステムの一貫性
- アクセシビリティ対応

### パフォーマンス
- チャートの遅延ローディング
- データキャッシング戦略
- モバイル最適化

### セキュリティ
- 目標データの所有権検証
- レート制限の適用
- 入力バリデーション

## 実装ステップ（Pattern 1）

### Week 1: コアチャートコンポーネント

**Day 1-2: WeeklyTrendChart**
1. `components/dashboard/WeeklyTrendChart.tsx` 作成
2. `/stats/calendar` APIからデータ取得
3. 最新7-14日分をフィルタリング
4. Recharts LineChart実装
5. グラデーション塗りつぶし追加
6. アニメーション実装

**Day 3-4: ScoreDistributionChart**
1. `components/dashboard/ScoreDistributionChart.tsx` 作成
2. `/failures` APIから最新100件取得
3. スコア分布を計算（1-5点）
4. Recharts PieChart実装
5. 中央に最頻スコア表示

**Day 5-6: CalendarHeatmap**
1. `components/dashboard/CalendarHeatmap.tsx` 作成
2. GitHub風レイアウト実装
3. failure_countに基づく色の濃淡
4. ホバーで日別詳細表示
5. クリックでその日の失敗一覧へ遷移

**Day 7: 統合とテスト**
1. `app/(dashboard)/page.tsx` にチャート統合
2. `StatsCard.tsx` にトレンド指標追加
3. ローディングスケルトン実装
4. レスポンシブ対応確認

### Week 2: 最適化とポリッシュ

1. モバイル表示の最適化
2. パフォーマンスチューニング
3. クロスブラウザテスト
4. エラーハンドリング強化
5. プロダクション展開

## 実装ロードマップ（確定版）

### フェーズ1: 視覚化（1-2週間）
1. `npm install framer-motion` でパッケージ追加
2. WeeklyTrendChart 実装
3. ScoreDistributionChart 実装
4. CalendarHeatmap 実装
5. ダッシュボードに統合
6. モバイル最適化とテスト
7. プロダクション展開

### フェーズ1.5: ストリーク機能（3-5日）
1. バックエンド `/stats/streak` API実装
2. テスト追加
3. StreakTracker コンポーネント実装
4. ダッシュボードに統合
5. プロダクション展開

### フェーズ2以降（オプション）
効果測定の結果に基づいて、目標設定やバッジ機能の実装を検討

## Critical Files

### フェーズ1: Pattern 1（視覚化）実装

**既存ファイル（変更）**:
- `frontend/app/(dashboard)/page.tsx` - チャート統合
- `frontend/components/dashboard/StatsCard.tsx` - トレンド指標追加
- `frontend/lib/types/index.ts` - 型定義追加
- `frontend/package.json` - framer-motion追加

**新規ファイル**:
- `frontend/components/dashboard/WeeklyTrendChart.tsx`
- `frontend/components/dashboard/ScoreDistributionChart.tsx`
- `frontend/components/dashboard/CalendarHeatmap.tsx`
- `frontend/components/dashboard/ChartSkeleton.tsx`

### フェーズ1.5: ストリーク機能実装

**バックエンド**:
- `backend/main.py` - `/stats/streak` エンドポイント追加
- `backend/tests/test_stats.py` - ストリークテスト追加
- `backend/schemas.py` - StreakResponse スキーマ追加

**フロントエンド**:
- `frontend/components/dashboard/StreakTracker.tsx` - 新規作成
- `frontend/app/(dashboard)/page.tsx` - StreakTracker統合

### フェーズ2以降（オプション）

**その他のゲーミフィケーション機能**:
- 目標設定（Goal CRUD API、GoalDashboard コンポーネント）
- バッジシステム（Achievement API、AchievementBadges コンポーネント）
- 週次サマリー（WeeklySummary コンポーネント）

---

## 参考資料

- プランファイル: `~/.claude/plans/tender-munching-stonebraker.md`
- プロジェクトドキュメント: `CLAUDE.md`
- フロントエンド: `frontend/`
- バックエンド: `backend/`
